#!/usr/bin/env ruby
# frozen_string_literal: true

require_relative '../lib/cli'

case ARGV.size
when 1
  if ARGV.first == '' || ARGV.first !~ /(.*help)|(.*-h)/
    CLI.new.execute(ARGV.first)
  else
    CLI.new.help
  end
else
  CLI.new.help
end
